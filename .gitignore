node_modules/

.DS_Store
*/.DS_Store

# Firebase config / 環境変数 — 実キーが入るため git 管理外
# 注意: .env.example（プレースホルダのテンプレート）は追跡する。以下は実キー用のみ除外。
firebase-config.js
.env
.env.local
.env.*.local
*.pem
*.key
*credentials*
*secret*

# ビルド成果物
dist/
dist-ssr/
*.tsbuildinfo

# Claude メモリ・ログ・内部ドキュメント（設計書・計画書など公開リポジトリに含めない）
.remember/
.claude/
docs/
*.plan.md
CLAUDE.md
